{
  "name": "daily-diet-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n api,web -c blue,magenta \"npm --prefix backend run start\" \"npm --prefix frontend run preview\"",
    "studio": "npm --prefix backend run studio",
    "migrate": "npm --prefix backend run migrate",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
